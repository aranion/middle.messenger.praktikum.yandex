<form id="{{formName}}">
    <div class="editProfile__name">{{nameUser}}</div>
    <div class="editProfile__row editProfile__row_params">
        {{#each EditProfileField}}
            {{{this}}}
        {{/each}}
    </div>
    <div class="editProfile__button_wrapper">
        {{{ButtonSave}}}
    </div>
</form>